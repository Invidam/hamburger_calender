import * as React from 'react';
export default class StarRatingInput extends React.Component<Props, State> {
    private _promiseSetStateDone;
    readonly promiseSetStateDone: Promise<void>;
    constructor(props: Props);
    render(): JSX.Element;
    private starProps;
    private anchorMode;
    private handleStarMouseEnter;
    private handleStarMouseLeave;
    private handleStarClick;
}
export interface Props {
    size?: number;
    value: number;
    onChange: (value: number) => void;
}
export interface State {
    prospectiveValue: number;
}
